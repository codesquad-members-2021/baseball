import { useState, useEffect } from 'react';

const useGameSelect = () => {

  const [userSelect, setUserSelect] = useState(null);
  const [userSelectable, setUserSelectable] = useState(null);

  return (
    <div>
      
    </div>
  )
}

export default useGameSelect
