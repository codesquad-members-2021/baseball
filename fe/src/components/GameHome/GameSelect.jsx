
const GameSelect = ({home, away, isStart}) => {
    return (
        <div>
            <span>{home}</span>
            <span>vs</span>
            <span>{away}</span>
        </div>
    )
}

export default GameSelect
