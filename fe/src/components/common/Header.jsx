import styled from "styled-components";

const Header = () => {
  return <HeaderLayout>BASEBALL GAME ONLINE</HeaderLayout>;
};

const HeaderLayout = styled.div`
  width: 100%;
  font-size: 5rem;
  color: white;
`;

export default Header;
