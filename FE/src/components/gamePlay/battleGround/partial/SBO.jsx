import styled from "styled-components";

const SBO = () => {
    return (
        <StyledSBO>
            SBO
        </StyledSBO>
    );
};

export default SBO;

// --- Styled Components ---
const StyledSBO = styled.div`
`;