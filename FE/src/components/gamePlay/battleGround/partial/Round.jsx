import styled from "styled-components";

const Round = () => {
    return (
        <StyledRound>
            Round
        </StyledRound>
    );
};

export default Round;

// --- Styled Components ---
const StyledRound = styled.div`
`;