import styled from "styled-components";

const MatchScreen = () => {
    return (
        <StyledMatchScreen>
            MatchScreen
        </StyledMatchScreen>
    );
};

export default MatchScreen;

// --- Styled Components ---
const StyledMatchScreen = styled.div`
`;