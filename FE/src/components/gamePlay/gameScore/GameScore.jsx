import styled from "styled-components";

const GameScore = () => {
    return (
        <StyledGameScore>
            Game Score
        </StyledGameScore>
    );
};

export default GameScore;

// --- Styled Components ---
export const StyledGameScore = styled.div`

`;