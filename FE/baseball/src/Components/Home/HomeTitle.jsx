import { HomeStyles as S } from '@/Components/Home/HomeStyles';
import { HOME_TITLE } from '@/Utils/const';

const HomeTitle = () => {
  return <S.HomeTitle>{HOME_TITLE}</S.HomeTitle>;
};

export default HomeTitle;
