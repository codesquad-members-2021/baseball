import { GameHeader as S } from '@/Components/Game/GameStyles';
import { HOME_TITLE } from '@/Utils/const';

const GameTitle = () => {
  return <S.GameTitle>{HOME_TITLE}</S.GameTitle>;
};

export default GameTitle;
